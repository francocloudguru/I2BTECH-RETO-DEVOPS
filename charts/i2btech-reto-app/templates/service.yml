apiVersion: v1
kind: Service
metadata:
  name: i2btech-reto-app3
spec:
  type: {{ .Values.service.type }}
  selector:
    app: i2btech-reto-app3
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 80
